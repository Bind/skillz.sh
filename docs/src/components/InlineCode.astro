---
interface Props {
  size?: "xs" | "sm";
  class?: string;
}

const { size = "sm", class: className } = Astro.props;
---

<code class:list={[
  "bg-gray-100 px-1.5 py-0.5 rounded font-mono text-gray-800",
  size === "xs" ? "text-xs" : "text-sm",
  className
]}><slot /></code>
